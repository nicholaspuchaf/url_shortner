pre-commit:
  jobs:
    - name: linter
      run: pnpm lint

    - name: lint fix
      run: pnpm lint:fix

    - name: test frontend
      run: cd frontend && pnpm test

    - name: test backend
      run: cd backend && python3 -m unittest tests/test_handler.py
