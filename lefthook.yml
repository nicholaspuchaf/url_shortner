pre-commit:
  jobs:
    - name: codecheck
      run: pnpm codecheck

    - name: linter
      run: pnpm lint

    - name: lint fix
      run: pnpm lint:fix

    - name: test frontend
      run: cd frontend && pnpm test
